# Extraction backend: "claude" (vision) or "local" (offline, pdfplumber + geometry)
EXTRACTOR=claude

ANTHROPIC_API_KEY=
ANTHROPIC_AUTH_TOKEN=

# Claude model used for vision extraction
CLAUDE_MODEL=claude-opus-4-8

# Where uploaded and annotated PDFs are stored
STORAGE_DIR=storage
